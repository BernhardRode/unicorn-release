name: 🧪 Manually deploy tag to a environment

on:
  workflow_dispatch:
    inputs:
      target-tag:
        description: 'Tag to deploy'
        default: 'v2.0.0'
        required: true
      target-env:
        description: 'Environment to deploy to'
        type: choice
        options:
          - development
          - testing
          - staging
          - production
        required: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.event.inputs.target-env }}
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/deploy-swa
        with:
          deployment-token: secrets.DEPLOYMENT_TOKEN
          github-token: secrets.GITHUB_TOKEN
          target-ref: ${{ github.event.inputs.target-tag }}
