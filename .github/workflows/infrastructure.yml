name: Reusable Terraform

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

env:
  # verbosity setting for Terraform logs
  TF_LOG: INFO

permissions:
  id-token: write
  contents: read

jobs:
  terraform:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
        # We keep Terraform files in the terraform directory.
        working-directory: ./terraform
    steps:
      - name: Checkout the repository to the runner
        uses: actions/checkout@v3
      - name: Sets env vars for release (PR)
        if: ${{ github.ref != 'refs/heads/main' && github.event_name == 'pull_request' }}
        env:
          ENVIRONMENT: development-${{ format( github.head_ref, '/', '-') }}
        run: |
          echo "CPS_ENVIRONMENT=${ENVIRONMENT}" >> $GITHUB_ENV

      # - name: Sets env vars for release (DEVELOPMENT)
      #   if: github.ref == 'refs/heads/main'
      #   env:
      #     ENVIRONMENT: github.head.name
      #   run: |
      #     echo "CPS_ENVIRONMENT=${ENVIRONMENT}" >> $GITHUB_ENV

      # - name: Sets env vars for release (STAGING)
      #   if: github.ref == 'refs/heads/main' && github.event.name == 'pull_request'
      #   env:
      #     ENVIRONMENT: github.head.name
      #   run: |
      #     echo "CPS_ENVIRONMENT=${ENVIRONMENT}" >> $GITHUB_ENV
      # - name: Sets env vars for release (PRODUCTION)
      #   if: github.ref == 'refs/heads/main' && github.event.name == 'pull_request'
      #   env:
      #     ENVIRONMENT: github.head.name
      #   run: |
      #     echo "CPS_ENVIRONMENT=${ENVIRONMENT}" >> $GITHUB_ENV
      - name: Environment
        run: |
          env | sort
      # - name: Configure AWS Credentials
      #   uses: aws-actions/configure-aws-credentials@v1-node16
      #   with:
      #     role-to-assume: arn:aws:iam::828226612169:role/Cps-GitHub-Actions
      #     aws-region: eu-central-1

      # - name: Setup Terraform with specified version on the runner
      #   uses: hashicorp/setup-terraform@v2
      #   with:
      #     terraform_version: 1.3.6
      # - name: Terraform Format
      #   id: fmt
      #   run: terraform fmt -check
      #   continue-on-error: true
      # - name: Terraform Init
      #   id: init
      #   run: terraform init
      # - name: Terraform Validate
      #   id: validate
      #   run: terraform validate
      # - name: Terraform plan
      #   id: plan
      #   run: terraform plan -no-color -input=false
      #   continue-on-error: true
      # - uses: actions/github-script@v6
      #   if: github.event_name == 'pull_request'
      #   env:
      #     PLAN: "terraform\n${{ steps.plan.outputs.stdout }}"
      #   with:
      #     script: |
      #       const output = `#### Terraform Format and Style 🖌\`${{ steps.fmt.outcome }}\`
      #       #### Terraform Initialization ⚙️\`${{ steps.init.outcome }}\`
      #       #### Terraform Validation 🤖\`${{ steps.validate.outcome }}\`
      #       #### Terraform Plan 📖\`${{ steps.plan.outcome }}\`

      #       <details><summary>Show Plan</summary>

      #       \`\`\`\n
      #       ${process.env.PLAN}
      #       \`\`\`

      #       </details>
      #       *Pushed by: @${{ github.actor }}, Action: \`${{ github.event_name }}\`*`;

      #       github.rest.issues.createComment({
      #         issue_number: context.issue.number,
      #         owner: context.repo.owner,
      #         repo: context.repo.repo,
      #         body: output
      #       })
      # - name: Terraform Format incorrect
      #   if: steps.fmt.outcome
      #   run: exit 1
      #   # run: terraform fmt
