name: 🚀 Update Releases

on:
  push:
    branches:
      - main

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - name: Git Release
        uses: anton-yurchenko/git-release@v5.0.0
